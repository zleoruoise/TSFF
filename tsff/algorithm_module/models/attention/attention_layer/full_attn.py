import torch
from torch import nn
import torch.nn.functional as F
from ....models.builder import ATTENTION_LAYERS, build_attention_layer
#from ...model_utils import TriangularCausalMask


import numpy as np


@ATTENTION_LAYERS.register_module()
class FullAttention(nn.Module):
    def __init__(self, mask_flag=True, factor=5, scale=None, attention_dropout=0.1, output_attention=False):
        super(FullAttention, self).__init__()
        self.scale = scale
        self.mask_flag = mask_flag
        self.output_attention = output_attention
        self.dropout = nn.Dropout(attention_dropout)
        
    def forward(self, queries, keys, values, attn_mask):
        B, L, H, E = queries.shape
        _, S, _, D = values.shape
        scale = self.scale or 1./np.sqrt(E)

        scores = torch.einsum("blhe,bshe->bhls", queries, keys)
        if self.mask_flag:
            if attn_mask is None:
                attn_mask = TriangularCausalMask(B, L, device=queries.device)

            scores.masked_fill_(attn_mask.mask, -np.inf)

        A = self.dropout(torch.softmax(scale * scores, dim=-1))
        V = torch.einsum("bhls,bshd->blhd", A, values)
        
        if self.output_attention:
            return (V.contiguous(), A)
        else:
            return (V.contiguous(), None)

@ATTENTION_LAYERS.register_module()
class AttentionLayer(nn.Module):
    def __init__(self, attn_type, d_model, nhead, 
                 d_keys=None, d_values=None, mix=False):
        super(AttentionLayer, self).__init__()

        d_keys = d_keys or (d_model//nhead)
        d_values = d_values or (d_model//nhead)

        self.inner_attention = build_attention_layer(attn_type)
        self.query_projection = nn.Linear(d_model, d_keys * nhead)
        self.key_projection = nn.Linear(d_model, d_keys * nhead)
        self.value_projection = nn.Linear(d_model, d_values * nhead)
        self.out_projection = nn.Linear(d_values * nhead, d_model)
        self.n_heads = nhead
        self.mix = mix

    def forward(self, queries, keys, values, attn_mask):
        B, L, _ = queries.shape
        _, S, _ = keys.shape
        H = self.n_heads

        queries = self.query_projection(queries).view(B, L, H, -1)
        keys = self.key_projection(keys).view(B, S, H, -1)
        values = self.value_projection(values).view(B, S, H, -1)

        out, attn = self.inner_attention(
            queries,
            keys,
            values,
            attn_mask
        )
        if self.mix:
            out = out.transpose(2,1).contiguous()
        out = out.view(B, L, -1)

        return self.out_projection(out), attn